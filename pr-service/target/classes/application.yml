server:
  port: 8083

spring:
  application:
    name: pr-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/pr_db}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
