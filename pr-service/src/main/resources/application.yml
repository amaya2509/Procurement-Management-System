server:
  port: 8083

spring:
  application:
    name: pr-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27019/pr_db?w=1}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
