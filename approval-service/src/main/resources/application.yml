server:
  port: 8085

spring:
  application:
    name: approval-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27021/approval_db?w=1}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
