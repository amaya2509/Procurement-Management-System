server:
  port: 8085

spring:
  application:
    name: approval-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/approval_db}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
