server:
  port: 8081

spring:
  application:
    name: user-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/user_db}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
