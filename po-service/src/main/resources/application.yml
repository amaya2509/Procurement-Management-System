server:
  port: 8084

spring:
  application:
    name: po-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27020/po_db?w=1}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
