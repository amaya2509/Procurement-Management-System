server:
  port: 8082

spring:
  application:
    name: supplier-service
  data:
    mongodb:
      uri: ${MONGO_URI:mongodb://localhost:27017/supplier_db}

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/
